
#Power settings
[power]

	#Generator settings
	[power.generator]
		#Maximum power for the Generator
		#Range: > 0
		maxPower = 100000
		#Power generation to send per tick
		#Range: > 0
		transfer = 2000
		#Produced FE per tick
		#Range: > 0
		produce = 4
		#Ticks per coal block
		#Range: > 0
		ticks = 400

	#Cell Extractor settings
	[power.cell_extractor]
		#Maximum power for the Cell Extractor
		#Range: > 0
		maxPower = 100000
		#Power to transfer per tick
		#Range: > 0
		extract = 1000
		#Power to consume per tick
		#Range: > 0
		consume = 2
		#Tick count per process
		#Range: > 0
		ticks = 100

	#DNA Extractor settings
	[power.dna_extractor]
		#Maximum power for the DNA Extractor
		#Range: > 0
		maxPower = 100000
		#Power to transfer per tick
		#Range: > 0
		extract = 1000
		#Power to consume per tick
		#Range: > 0
		consume = 2
		#Tick count per process
		#Range: > 0
		ticks = 100

	#DNA Injector settings
	[power.dna_editor]
		#Maximum power for the DNA Injector
		#Range: > 0
		maxPower = 100000
		#Power to transfer per tick
		#Range: > 0
		extract = 1000
		#Power to consume per tick
		#Range: > 0
		consume = 2
		#Tick count per process
		#Range: > 0
		ticks = 100

	#Cloning Machine settings
	[power.cloning_machine]
		#Maximum power for the Cloning Machine
		#Range: > 0
		maxPower = 100000
		#Power to transfer per tick
		#Range: > 0
		extract = 2000
		#Power to consume per tick
		#Range: > 0
		consume = 3
		#Tick count per process
		#Range: > 0
		ticks = 1000

	#Analyzer settings
	[power.analyzer]
		#Maximum power for the Analyzer
		#Range: > 0
		maxPower = 100000
		#Power generation to send per tick
		#Range: > 0
		transfer = 1000
		#Power to consume per tick
		#Range: > 0
		consume = 2
		#Tick count per process
		#Range: > 0
		ticks = 100

